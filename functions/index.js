const { initializeApp } = require("firebase-admin/app");

initializeApp();

// Callables land here in later Phase 1b milestones: the debug custom-token
// minting path (Milestone 2) and the game-truth inventory — createGame,
// castVote, proposeElimination, etc. (Milestone 4). See
// implementation_plan.md's "Cloud Functions inventory" section.
